# 043 · docs: document Discord command set in README.md

**Status:** complete

## What changed
- `README.md`: added a **Discord commands** section linking to
  `DISCORD_COMMANDS.txt` and including a quick-reference table for the
  10 most-used commands plus prefix and restart notes

## Why
`DISCORD_COMMANDS.txt` had the full reference but wasn't linked from the
README. New users had no obvious entry point for the command interface.

## Verification
- `README.md` renders correctly in GitHub
- `DISCORD_COMMANDS.txt` link is valid (file exists in repo root)
