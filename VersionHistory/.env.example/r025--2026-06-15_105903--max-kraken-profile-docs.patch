--- a/.env.example
+++ b/.env.example
@@ -316,10 +316,48 @@
 # See docs/kraken-equities-futures.md for live allowlist and futures roadmap.
 # ---------------------------------------------------------------------------
 ENABLE_EQUITIES=0
-# EQUITY_WATCHLIST=AAPLx,TSLAx,SPYx
+# EQUITY_WATCHLIST=AAPLx,TSLAx,SPYx,NVDAx,MSFTx,GOOGLx
 # Max portfolio share for any single equity/ETF (separate from MAX_ALT_ALLOCATION_PCT)
 # MAX_EQUITY_ALLOCATION_PCT=0.15
-# Live: add approved tickers to LIVE_ALLOWED_ASSETS (e.g. ETH,ADA,AAPLx) — never auto-enabled
+# Live: add approved tickers to LIVE_ALLOWED_ASSETS (e.g. ETH,ADA,AAPLx,TSLAx,SPYx)
+
+# ---------------------------------------------------------------------------
+# Kraken Futures (perpetuals) — paper sim v1 (default OFF)
+# Separate wallet from spot. Requires ccxt.krakenfutures.
+# Live futures needs LIVE_ENABLED=1 + LIVE_FUTURES_ENABLED=1 + confirm phrase.
+# Same drawdown halt (LIVE_DRAWDOWN_HALT_PCT) applies to futures paper/live.
+# ---------------------------------------------------------------------------
+ENABLE_FUTURES=0
+# LIVE_FUTURES_ENABLED=0
+# ccxt swap symbols (crypto + xStock perps when available):
+# FUTURES_WATCHLIST=BTC/USD:USD,ETH/USD:USD,AAPLX/USD:USD,TSLAX/USD:USD,SPYX/USD:USD
+# FUTURES_MAX_LEVERAGE=5
+# FUTURES_MAX_POSITION_USD=100
+# FUTURES_PAPER_BALANCE_USD=1000
+# FUTURES_PAPER_STATE_FILE=.futures_paper_state.json
+# FUTURES_LIVE_STATE_FILE=.futures_live_state.json
+# RESET_FUTURES_STATE=0
+
+# ---------------------------------------------------------------------------
+# Max Kraken profile (aggressive spot + equities + arb, guardrails on)
+# Copy non-secret knobs into .env — never commit .env.
+# ---------------------------------------------------------------------------
+# ENABLE_EQUITIES=1
+# EQUITY_WATCHLIST=AAPLx,TSLAx,SPYx,NVDAx,MSFTx
+# LIVE_ENABLED=1
+# LIVE_TRADING_CONFIRM=I_ACCEPT_REAL_MONEY
+# LIVE_MIRROR_PAPER=1
+# LIVE_ALLOWED_ASSETS=ETH,ADA,BTC,DOT,UNI,AAVE,SOL,LINK,AAPLx,TSLAx,SPYx
+# LIVE_ALLOW_TRIANGULAR=1
+# LIVE_MAX_ROUTE_LEGS=3
+# LIVE_MIRROR_MIN_CONFIDENCE=confirm
+# LIVE_STRICT_PROFIT=1
+# PROFIT_ONLY_MODE=1
+# LIVE_DRAWDOWN_HALT_PCT=0.10
+# ENABLE_FUTURES=1
+# STRATEGIES=cross_momentum,triangular_arbitrage,stat_arb
+# Kraken spot margin: NOT enabled — requires separate leverage/liquidation model.
+# See docs/kraken-equities-futures.md#spot-margin-limitation
 
 # ---------------------------------------------------------------------------
 # Whale watch — alert-only monitoring of large Kraken trades + volume spikes.
