--- a/.env.example
+++ b/.env.example
@@ -259,6 +259,8 @@
 LIVE_ENABLED=0
 # LIVE_STATE_FILE=.live_state.json
 # LIVE_MAX_USD_PER_TRADE=75
+# Stop after N successful live trades (0 = unlimited)
+# LIVE_MAX_TRADES=0
 # Hard halt all trading when portfolio drops this much from session peak
 LIVE_DRAWDOWN_HALT_PCT=0.10
 # Untouchable ETH floor — live trading halts if balance drops below this
