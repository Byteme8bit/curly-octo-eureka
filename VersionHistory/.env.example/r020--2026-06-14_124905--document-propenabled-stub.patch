--- a/.env.example
+++ b/.env.example
@@ -293,6 +293,10 @@
 # LIVE_MIRROR_PAPER=0
 # RESET_LIVE_STATE=0
 
+# Kraken Trade Prop (evaluation / funded accounts) — NOT IMPLEMENTED.
+# TradeBot uses spot wallet only. See docs/kraken-prop.md.
+# PROP_ENABLED=0
+
 # ---------------------------------------------------------------------------
 # Whale watch — alert-only monitoring of large Kraken trades + volume spikes.
 # No extra API keys; uses public Kraken market data the bot already reaches.
