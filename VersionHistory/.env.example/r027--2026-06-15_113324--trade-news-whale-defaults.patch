--- a/.env.example
+++ b/.env.example
@@ -63,7 +63,7 @@
 SLIPPAGE_BUFFER_PCT=0.0005
 MIN_NET_PROFIT_PCT=0.0005
 # Block offensive entries when preflight net <= 0 (defensive exits still allowed)
-# PROFIT_ONLY_MODE=0
+PROFIT_ONLY_MODE=1
 # Faster cadence defaults when PROFIT_ONLY_MODE=1 (only affects unset sizing knobs)
 # YOLO_PROFITABLE=0
 MOMENTUM_TIMEFRAMES=15m,1h
@@ -401,6 +401,19 @@
 WHALE_FOLLOW_MIN_NET_PROFIT=0.0005
 
 # ---------------------------------------------------------------------------
+# Trade decision stack — news + market flow consulted before offensive trades.
+# Whale-follow and equity DCA bypass these gates (whales are the signal; DCA
+# is scheduled accumulation). Defensive trims always allowed.
+# ---------------------------------------------------------------------------
+TRADE_NEWS_CHECK_ENABLED=1
+TRADE_NEWS_BLOCK_SEVERE=1
+# Block DCA on severe headlines too (default OFF — DCA is intentional accumulation)
+TRADE_NEWS_BLOCK_DCA=0
+TRADE_FLOW_CHECK_ENABLED=1
+TRADE_FLOW_MOMENTUM_THRESHOLD=-0.008
+TRADE_FLOW_RISK_OFF_RATIO=0.55
+
+# ---------------------------------------------------------------------------
 # Goal evolution — portfolio milestones unlock strategies gradually.
 # Default ON. State: .tradebot_goals_state.json
 # **Goal 1** = $10,000 portfolio (Growth tier) — primary milestone; unlocks stat_arb
