--- a/.env.example
+++ b/.env.example
@@ -302,12 +302,16 @@
 WHALE_FOLLOW_MIN_NET_PROFIT=0.0005
 
 # ---------------------------------------------------------------------------
-# Goal evolution — portfolio milestones unlock strategies gradually (paper).
+# Goal evolution — portfolio milestones unlock strategies gradually.
 # Default ON. State: .tradebot_goals_state.json
+# **Goal 1** = $10,000 portfolio (Growth tier) — primary milestone; unlocks stat_arb
+# Goal 2 = $100k (+ triangular_arbitrage, higher exploration)
+# Goal 3 = $1M (full multi-strategy + whale-follow sizing bump)
 # Tier 0 (<$10k): cross_momentum only
 # Tier 1 ($10k+): + stat_arb
 # Tier 2 ($100k+): + triangular_arbitrage, higher exploration
 # Tier 3 ($1M+): full multi-strategy + whale-follow sizing bump
+# In LIVE_MIRROR_PAPER mode the bot tracks live Kraken portfolio for goals.
 # ---------------------------------------------------------------------------
 GOAL_EVOLUTION_ENABLED=1
 # GOAL_STATE_FILE=.tradebot_goals_state.json
